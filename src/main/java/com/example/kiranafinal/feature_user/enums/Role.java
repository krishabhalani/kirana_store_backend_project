package com.example.kiranafinal.feature_user.enums;

/**
 * Enum representing user roles in the system.
 */
public enum Role {
    /**
     * Role for administrators with higher privileges.
     */
    ADMIN,

    /**
     * Role for regular users with standard access.
     */
    USER
}
